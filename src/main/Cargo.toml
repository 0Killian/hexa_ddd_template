[package]
name = "main"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.5.21", features = ["derive"] }
tokio = { version = "1.41.1", features = ["full"] }
tracing = "0.1.40"
http = { path = "../interface/http/server" }
common = { path = "../core/libs/common" }
