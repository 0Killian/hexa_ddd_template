[package]
name = "ui"
version = "0.1.0"
edition = "2021"

[dependencies]
strum = { version = "0.26.3", features = ["derive"] }
yew = { version = "0.21.0", features = ["ssr"] }
common = { path = "../common" }
entities = { path = "../../entities" }
